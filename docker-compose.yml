version: '3'

services:
  api:
    build:
      context: EmailApp
    image: emailapp:3.0
    ports:
      - "5000:80"
    environment:
      - AuthRequired=false
    # file should contain:
    # ConnectionStrings__EmailDb=(connection string)
    env_file:
      - email-db-connection.env
